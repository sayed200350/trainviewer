import AppIntents
import Foundation

// MARK: - Main Extension Entry Point
@main
struct TrainViewerAppIntents {
    static func main() {
        // Extension entry point - no additional setup needed
        // The system will discover and register our intents automatically
    }
}
